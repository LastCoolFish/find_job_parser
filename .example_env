# PostgreSQL
DB_USER=db_user_name
DB_PASSWORD=db_user_password
HOST=localhost
PORT=5432
DB_NAME=FindJob

# Playwright: True in Docker/production (no display available); False locally to watch the browser
HEADLESS=True


